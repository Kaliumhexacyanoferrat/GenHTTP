# GenHTTP.Modules.Authentication

This package adds authentication method
implementations to GenHTTP applications.

Currently, API key, basic, bearer and
client authentication are supported.

For examples as well as role based authentication,
please refer to [the documentation](https://genhttp.org/documentation/content/concerns/authentication/).
