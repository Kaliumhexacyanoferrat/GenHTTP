dist: xenial

addons:
  snaps:
  - name: dotnet-sdk
    classic: true
    channel: latest/beta
    
sudo: required

language: csharp
mono: none

solution: GenHTTP.sln

script:
  - sudo snap alias dotnet-sdk.dotnet dotnet
  - dotnet restore
  - dotnet test
